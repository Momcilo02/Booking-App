<Window x:Class="BookingApp.View.Guide.LiveTourPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.View.Guide"
        mc:Ignorable="d"
        Title="LiveTourPage" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Checkpoints:" Margin="5" FontWeight="Bold" FontSize="16"/>

        <ScrollViewer Grid.Row="1">
            <StackPanel x:Name="checkpointPanel" Orientation="Vertical"/>
        </ScrollViewer>

        <TextBlock Grid.Row="2" Text="Tourists:" Margin="5" FontWeight="Bold" FontSize="16"/>

        <ScrollViewer Grid.Row="3">
            <StackPanel x:Name="touristPanel" Orientation="Vertical"/>
        </ScrollViewer>
    </Grid>
</Window>
