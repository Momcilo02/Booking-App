<Window x:Class="BookingApp.WPF.View.Owner.YearStats"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.WPF.View.Owner"
        mc:Ignorable="d"
        Title="YearStats" Height="450" Width="800">
    <Grid>
        <StackPanel Orientation="Horizontal" Margin="10,-43,0,53">
            <Label Content="Godina 2020 :" VerticalAlignment="Center"/>
            <TextBox x:Name="txtYear" Width="100" Height="20" VerticalAlignment="Center" Text="{Binding stat1.Reservations}"/>
            <TextBox x:Name="txtYear1" Width="100" Height="20" VerticalAlignment="Center" Text="{Binding stat1.Cancellations}"/>
            <TextBox x:Name="txtYear2" Width="100" Height="20" VerticalAlignment="Center" Text="{Binding stat1.Reschedulings}"/>
            <TextBox x:Name="txtYear3" Width="100" Height="20" VerticalAlignment="Center" Text="{Binding stat1.RenovationRecommendations}"/>
            <Button Content="Detalji"  Height="20" Margin="10" Command="{Binding Path=NavigationClick}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="5">
            <Label Content="Godina 2021 :" VerticalAlignment="Center"/>
            <TextBox x:Name="txtYear21" Width="100" Height="20" VerticalAlignment="Center" Text="{Binding stat2.Reservations}"/>
            <TextBox x:Name="txtYear121" Width="100" Height="20" VerticalAlignment="Center" Text="{Binding stat2.Cancellations}"/>
            <TextBox x:Name="txtYear221" Width="100" Height="20" VerticalAlignment="Center" Text="{Binding stat2.Reschedulings}"/>
            <TextBox x:Name="txtYear321" Width="100" Height="20" VerticalAlignment="Center" Text="{Binding stat2.RenovationRecommendations}"/>
            <Button Content="Detalji"  Height="20" Margin="10" Command="{Binding Path=NavigationClick1}"/>
        </StackPanel>
    </Grid>
</Window>
